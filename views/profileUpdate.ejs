<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Profile</title>
    <link rel="stylesheet" href="/style.css">
</head>

<body>
    <%- include('partials/_navbar') %>

    <h1>Update Profile</h1>

    <% if (error) { %>
    <section>
        <p class="error"><%= error %></p>
    </section>
    <% } %>

    <form action="/user-details/edit/<%= id %>" method="POST">
        <table>
            <tr>
                <td><label>First Name</label></td>
                <td><input type="text" name="firstName" value="<%= userDetail?.firstName %>"></td>
            </tr>
            <tr>
                <td><label>Last Name</label></td>
                <td><input type="text" name="lastName" value="<%= userDetail?.lastName %>"></td>
            </tr>
            <tr>
                <td><label>Phone Number</label></td>
                <td><input type="text" name="phoneNumber" value="<%= userDetail?.phoneNumber %>"></td>
            </tr>
            <tr>
                <td><label>Date Of Birth</label></td>
                <td><input type="date" name="dateOfBirth" value="<%= userDetail?.date %>"></td>
            </tr>
            <tr>
                <td><label>Address</label></td>
                <td><textarea name="address"><%= userDetail?.address %></textarea></td>
            </tr>
        </table> <br>

        <button type="submit">Update</button>
    </form>

    <%- include('partials/_footer', { menu: "Update Profile" }) %>
</body>

</html>